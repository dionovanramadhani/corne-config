# Enable deep sleep (kunci utama hemat baterai)
CONFIG_ZMK_SLEEP=y
# Masuk deep sleep setelah 15 menit tidak ada input (900.000 ms)
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

# Perpendek durasi layar menyala saat idle (15 detik) untuk menghemat layar OLED
CONFIG_ZMK_IDLE_TIMEOUT=15000

# Nonaktifkan boost TX power untuk menghemat baterai radio BLE (default 0 dBm)
# CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Enable eager debouncing
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=1
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=10

# Enable ZMK Studio for Realtime Keymap Updates
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n

# Uncomment the following line to enable USB Logging (this increases power usage by a significant amount, turn it off when not in use)
# CONFIG_ZMK_USB_LOGGING=y

# Display & Battery Status (Shared)
CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_EXT_POWER=y
CONFIG_ZMK_WIDGET_BATTERY_STATUS_SHOW_PERCENTAGE=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y

